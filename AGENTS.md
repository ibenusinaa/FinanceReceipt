# AGENTS.md — Finance Receipt

## State

- **Empty repository.** No commits, no source files, no config.
- This file should be updated as the project takes shape.

## When to update this file

- After the first meaningful code is added (entrypoints, framework config, test setup).
- After build/lint/test tooling is configured.
- After CI or pre-commit hooks are added.
- Whenever a non-obvious convention or gotcha emerges.

## Next-session priorities

1. Read `README*`, root config files (package.json, Cargo.toml, etc.), and this file.
2. Run lint/typecheck/test commands before committing.
3. Check for `opencode.json` for repo-specific OpenCode config.
